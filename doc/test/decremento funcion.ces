entero z;

entero decrementa (entero x) {
	entero y = 8;
	y = x+1;
	{
		entero s = 2,h = 3;
		s = h + 1;
	}
	devuelve y;
}

vacio principal () { /* Función principal */
	/* variables locales*/
	entero w = 9;
	escribe("*** INVOCACION DE SUBPROGRAMAS***");
	z = 3;
	w = decrementa( decrementa(z) ); /* expresión de invocación de un subprograma */
	escribe("w (2) = ");
	escribeEnt(w);
	escribe("FIN");
}